# Copy this file to .env and fill values. NEVER commit your real keys.
# Gemini API key (use header X-goog-api-key) — optional if you use Gemini
GEMINI_API_KEY=your_gemini_api_key_here

# HuggingFace token (fallback if Gemini not provided)
HF_TOKEN=your_huggingface_token_here

# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/your_db_name

# Server port (optional)
PORT=5000
